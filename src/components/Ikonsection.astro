---
// src/components/Ikonsection.astro
// Props
const { number, text, image } = Astro.props;
---

<section class="grid_1-1-1">
  <div class="statistik-card">
    <img src={image} alt="Bowl ikon" class="ikon" />
    <p class="nummer">{number}</p>
    <p class="tekst">{text}</p>
  </div>
</section>

<style>
  .grid_1-1-1 {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    /* border-top: 2px solid #000;
    border-bottom: 2px solid #000; */
  }

  .statistik-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .ikon {
    width: 80px;
    height: auto;
    margin-bottom: 8px;
  }

  .nummer {
    font-size: 2rem;
    font-weight: bold;
    margin: 4px 0;
  }

  .tekst {
    font-size: 1rem;
    color: #333;
  }
</style>
